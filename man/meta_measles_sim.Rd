% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metaODIN_measles.R
\name{meta_measles_sim}
\alias{meta_measles_sim}
\title{Metapopulation Measles Simulator (ODIN Core)}
\usage{
meta_measles_sim(
  N_pop = 1,
  beta,
  S0 = rep(1000, N_pop),
  V10 = rep(0, N_pop),
  V20 = rep(0, N_pop),
  E10 = rep(0, N_pop),
  E20 = rep(0, N_pop),
  I1_Q0 = rep(0, N_pop),
  I1_U0 = rep(10, N_pop),
  I2_Q0 = rep(0, N_pop),
  I2_U0 = rep(0, N_pop),
  R0 = rep(0, N_pop),
  m_weekday_day,
  m_weekday_night,
  m_weekend_day,
  m_weekend_night,
  start_day = 0,
  delta_t,
  de1,
  de2,
  di1,
  di2,
  prop_I1_Q,
  prop_I2_Q,
  V1_eff,
  V2_eff,
  nsteps,
  is.stoch = FALSE,
  seed = NULL,
  do_chk = FALSE,
  chk_time_steps = NULL,
  chk_file_names = NULL
)
}
\arguments{
\item{N_pop}{Integer. Number of subpopulations.}

\item{beta}{Numeric vector or scalar. Transmission rate by subpopulation.}

\item{S0}{Numeric vector of length \code{N_pop}. Initial susceptible counts.}

\item{E10}{Numeric vector of length \code{N_pop}. Initial first exposed stage counts.}

\item{E20}{Numeric vector of length \code{N_pop}. Initial second exposed stage counts.}

\item{I1_Q0}{Numeric vector of length \code{N_pop}. Initial \code{I1_Q} counts.}

\item{I1_U0}{Numeric vector of length \code{N_pop}. Initial \code{I1_U} counts.}

\item{I2_Q0}{Numeric vector of length \code{N_pop}. Initial \code{I2_Q} counts.}

\item{I2_U0}{Numeric vector of length \code{N_pop}. Initial \code{I2_U} counts.}

\item{R0}{Numeric vector of length \code{N_pop}. Initial recovered counts.}

\item{m_weekday_day}{Numeric matrix (\verb{N_pop x N_pop}). Weekday daytime mixing matrix.}

\item{m_weekday_night}{Numeric matrix (\verb{N_pop x N_pop}). Weekday nighttime mixing matrix.}

\item{m_weekend_day}{Numeric matrix (\verb{N_pop x N_pop}). Weekend daytime mixing matrix.}

\item{m_weekend_night}{Numeric matrix (\verb{N_pop x N_pop}). Weekend nighttime mixing matrix.}

\item{start_day}{Integer in \code{0:6}, where 0 is Monday.}

\item{delta_t}{Numeric. Simulation time step in days (typically \code{0.5}).}

\item{de1}{Numeric vector or scalar. Mean duration (days) in \code{E1}.}

\item{de2}{Numeric vector or scalar. Mean duration (days) in \code{E2}.}

\item{di1}{Numeric vector or scalar. Mean duration (days) in \code{I1}.}

\item{di2}{Numeric vector or scalar. Mean duration (days) in \code{I2}.}

\item{prop_I1_Q}{Numeric vector or scalar in \verb{[0, 1]}. Proportion entering \code{I1_Q} from \code{E2}.}

\item{prop_I2_Q}{Numeric vector or scalar in \verb{[0, 1]}. Proportion of \code{I1_U -> I2} transitions entering \code{I2_Q}.}

\item{nsteps}{Integer. Number of simulation steps.}

\item{is.stoch}{Logical. Included for API compatibility; this model currently
uses stochastic binomial transitions.}

\item{seed}{Integer or \code{NULL}. Included for API compatibility.}

\item{do_chk}{Logical. Included for API compatibility (checkpointing not implemented here).}

\item{chk_time_steps}{Integer vector or \code{NULL}. Included for API compatibility.}

\item{chk_file_names}{Character vector/list or \code{NULL}. Included for API compatibility.}
}
\value{
A \code{data.table} in long format with columns:
\describe{
\item{step}{Time-step index (\code{0:nsteps}).}
\item{time}{Continuous simulation time (\code{step * delta_t}).}
\item{disease_state}{Compartment/metric name (for example \code{S}, \code{E1}, \code{I1_U}, \code{I_all}, \code{n_E2I1}).}
\item{population_id}{Subpopulation id (integer).}
\item{value}{Compartment value at that time and subpopulation.}
}
}
\description{
Core ODIN-based simulation engine for a measles metapopulation model with
two exposed stages (\code{E1}, \code{E2}) and two infectious stages (\code{I1}, \code{I2}),
each split into quarantined (\verb{_Q}) and unquarantined (\verb{_U}) compartments.
}
\details{
Compartments evolve as:
\itemize{
\item \code{S -> E1 -> E2 -> I1_Q/I1_U -> I2_Q/I2_U -> R}
}

Force of infection is computed from unquarantined infectious individuals
(\code{I1_U + I2_U}) under time-varying day/night and weekday/weekend mixing.
}
\examples{
\donttest{
options(odin.verbose = FALSE)
N_pop <- 2
mix <- diag(N_pop)
out <- meta_measles_sim(
  N_pop = N_pop,
  beta = 0.7,
  S0 = c(1000, 900),
  I1_U0 = c(5, 3),
  m_weekday_day = mix,
  m_weekday_night = mix,
  m_weekend_day = mix,
  m_weekend_night = mix,
  delta_t = 0.5,
  de1 = 3, de2 = 3, di1 = 2, di2 = 3,
  prop_I1_Q = 0.2, prop_I2_Q = 0.4,
  nsteps = 20
)
head(out)
}

}
