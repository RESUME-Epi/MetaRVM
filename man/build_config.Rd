% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_config.R
\name{build_config}
\alias{build_config}
\title{Build a MetaRVM configuration}
\usage{
build_config(
  disease,
  population_init,
  mixing_matrices,
  disease_params,
  start_date,
  sim_length,
  nsim = 1L,
  nrep = 1L,
  run_id = NULL,
  vaccination = NULL,
  sub_disease_params = NULL,
  random_seed = NULL,
  delta_t = NULL,
  checkpoint_dir = NULL,
  checkpoint_dates = NULL,
  restore_from = NULL,
  output_file = NULL,
  return_object = FALSE,
  verbose = FALSE,
  suppress_odin_messages = FALSE
)
}
\arguments{
\item{disease}{Character. Disease to simulate. Supported values:
\code{"flu"}, \code{"measles"}.}

\item{population_init}{Character. Path to the population initialization CSV.}

\item{mixing_matrices}{Named list of paths to mixing-matrix CSV files.
Required keys: \code{weekday_day}, \code{weekday_night}, \code{weekend_day},
\code{weekend_night}.}

\item{disease_params}{Named list of disease parameters. Each element may be
a scalar, a per-population vector, or a distribution-spec list such as
\code{list(dist = "lognormal", mu = -0.7, sd = 0.1)}.}

\item{start_date}{Character (\code{"MM/DD/YYYY"}) or \link{Date}. Simulation start
date.}

\item{sim_length}{Positive integer. Number of days to simulate.}

\item{nsim}{Positive integer. Number of parameter draws (default \code{1}).}

\item{nrep}{Positive integer. Replicates per parameter draw (default \code{1}).}

\item{run_id}{Optional character identifier stored in run metadata.
Auto-generated from the current timestamp when \code{NULL}.}

\item{vaccination}{Optional character. Path to the vaccination schedule CSV
(flu only).}

\item{sub_disease_params}{Optional named list of per-category parameter
overrides. Structure mirrors the \verb{sub_disease_params:} YAML section, e.g.
\code{list(age = list(child = list(ts = 0.6)))}.}

\item{random_seed}{Optional integer seed for reproducibility. Auto-generated
when \code{NULL}.}

\item{delta_t}{Numeric time-step in days. Defaults to \code{0.5} when \code{NULL}.}

\item{checkpoint_dir}{Optional character. Directory for checkpoint files
(flu only).}

\item{checkpoint_dates}{Optional character vector of dates
(\code{"MM/DD/YYYY"}) at which to write checkpoints (flu only).}

\item{restore_from}{Optional character. Path to a checkpoint \code{.Rda} file to
restore from (flu only).}

\item{output_file}{Optional character. When provided the configuration is
written to this path as a YAML file. Paths inside the config are stored
as absolute paths so the file is self-contained regardless of working
directory.}

\item{return_object}{Logical. When \code{TRUE} the configuration is parsed and
returned as a \link{MetaRVMConfig} object. When \code{FALSE} (default) the raw
configuration list is returned (invisibly when \code{output_file} is also
given).}

\item{verbose}{Logical. Passed through to \code{\link[=parse_config]{parse_config()}} when
\code{return_object = TRUE}.}

\item{suppress_odin_messages}{Logical. Passed through to \code{\link[=parse_config]{parse_config()}}
when \code{return_object = TRUE}.}
}
\value{
A \link{MetaRVMConfig} object when \code{return_object = TRUE}; otherwise the
raw configuration list (invisibly if \code{output_file} was also written).
}
\description{
Constructs a MetaRVM configuration programmatically. The argument structure
mirrors the YAML configuration file format. The result can be returned as a
raw list, written to a YAML file, or parsed directly into a
\link{MetaRVMConfig} object — or any combination of the three.
}
\examples{
ext <- function(x) system.file("extdata", x, package = "MetaRVM")

# Return the raw config list for inspection / modification
cfg <- build_config(
  disease         = "flu",
  population_init = ext("population_init_n24.csv"),
  mixing_matrices = list(
    weekday_day   = ext("m_weekday_day.csv"),
    weekday_night = ext("m_weekday_night.csv"),
    weekend_day   = ext("m_weekend_day.csv"),
    weekend_night = ext("m_weekend_night.csv")
  ),
  disease_params  = list(
    ts = 0.5, ve = 0.4, dv = 180, dp = 1, de = 3,
    da = 5,   ds = 6,   dh = 8,   dr = 180,
    pea = 0.3, psr = 0.95, phr = 0.97
  ),
  vaccination     = ext("vaccination_n24.csv"),
  start_date      = "10/01/2023",
  sim_length      = 30L,
  nsim            = 2L
)

# Parse directly to a MetaRVMConfig object
cfg_obj <- build_config(
  disease         = "flu",
  population_init = ext("population_init_n24.csv"),
  mixing_matrices = list(
    weekday_day   = ext("m_weekday_day.csv"),
    weekday_night = ext("m_weekday_night.csv"),
    weekend_day   = ext("m_weekend_day.csv"),
    weekend_night = ext("m_weekend_night.csv")
  ),
  disease_params  = list(
    ts = 0.5, ve = 0.4, dv = 180, dp = 1, de = 3,
    da = 5,   ds = 6,   dh = 8,   dr = 180,
    pea = 0.3, psr = 0.95, phr = 0.97
  ),
  vaccination     = ext("vaccination_n24.csv"),
  start_date      = "10/01/2023",
  sim_length      = 30L,
  return_object   = TRUE
)

}
\seealso{
\code{\link[=parse_config]{parse_config()}}, \code{\link[=metaRVM]{metaRVM()}}
}
